<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>FoodBazar</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        @keyframes fire {
            0% {
                text-fill-color: #f00;
                text-stroke-color: #f00;
                text-shadow: none;
            }
            25% {
                text-fill-color: #ff6600;
                text-stroke-color: #ff6600;
                text-shadow: 0 0 5px #ff6600;
            }
            50% {
                text-fill-color: #ffcc00;
                text-stroke-color: #ffcc00;
                text-shadow: 0 0 10px #ffcc00;
            }
            75% {
                text-fill-color: #f00;
                text-stroke-color: #f00;
                text-shadow: 0 0 15px #f00;
            }
            100% {
                text-fill-color: #f00;
                text-stroke-color: #f00;
                text-shadow: 0 0 20px #f00;
            }
        }

        @keyframes smoke {
            0% {
                opacity: 0;
            }
            20% {
                opacity: 1;
            }
            100% {
                opacity: 0;
            }
        }

        /* Additional styles for the page */
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #343a40;
        }

        .animation-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        @keyframes gradient {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        .animation-bg {
            background: linear-gradient(45deg, #EE7752, #E73C7E, #23A6D5, #23D5AB);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
        }

        .content {
            position: absolute;
            top: -10px;
            left: 12%;
            transform: translateX(-50%);
            text-align: center;
            z-index: 1;
        }

        h1 {
            font-size: 2rem;
            animation: fire 4s ease-in-out infinite, smoke 4s ease-in-out infinite;
            color: #fff;
        }

        .content1 {
            position: absolute;
            top: 8px;
            left: 95%;
            transform: translateX(-50%);
            text-align: left;
            z-index: 1;
        }

        .content2 {
            position: absolute;
            top: 4.5%;
            left: 30%;
            transform: translate(-50%, -50%);
            text-align: center;
            z-index: 1;
            width: 100%;
        }

        .search-box {
            display: inline-block;
            padding: 10px;
            position: relative;
        }

        .search-box input[type="text"] {
            width: 300px;
            padding: 10px;
            font-size: 1rem;
            border-radius: 20px; /* Increased border radius */
            border: none;
            outline: none;
            background-color: #f8f8f8; /* Light gray background */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Added box shadow */
            color: #333; /* Dark text color */
        }

        .search-box input[type="text"]::placeholder {
            color: #aaa; /* Light gray placeholder color */
        }

        .search-box::before {
            content: "";
           position: absolute;
           top: 100%;
           left: 0;
           width: 100%;
           height: 2px;
           background-color: transparent; /* Set the background color to transparent */
           transform: scaleX(0);
           transition: transform 0.3s ease;
}

        .search-box:hover::before,
        .search-box input[type="text"]:focus::before {
            transform: scaleX(1);
        }

        .search-box::after {
            content: "\f1d8"; /* FontAwesome send icon */
            position: absolute;
            top: 50%;
            right: 10px;
            transform: translateY(-50%);
            font-family: "Font Awesome 5 Free";
            font-weight: 900;
            font-size: 1rem;
            color: #131111; /* Light gray icon color */
            cursor: pointer; /* Add cursor style */
            margin-right: 1rem;
        }

        .logout-button {
          display: inline-block;
          padding: 10px 20px;
          font-size: 1rem;
          border-radius: 30px;
          border: none;
          outline: none;
          background-color: hwb(0 10% 90%);
          box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
          color: #efdddd;
          cursor: pointer;
          transition: background-color 0.3s ease;
}

       .logout-button:hover {
          background-color: #eee;
}

       .logout-button:active {
          background-color: #ddd;
}

       .logo {
            position: absolute;
            left: 30px;
            width: 70px;
            height: auto;
            animation: fire 4s ease-in-out infinite, smoke 4s ease-in-out infinite;
        }

    </style>
</head>
<body>
    <div class="animation-bg"></div>
    <img class="logo" src="./images/logo.png" alt="FoodBazar Logo">
    <div class="content">
        <h1>FoodBazar</h1>
    </div>
    <div class="content1">
        <form action="/logout?_method=DELETE" method="POST">
        <button class="logout-button" type="submit">LogOut</button>
          </form>
      </div>
    <div class="content2">
      <div class="search-box">
          <input type="text" placeholder="Search Food and Restaurant">
      </div>
    </body>
</html>
